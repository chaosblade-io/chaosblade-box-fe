// Example template for local development API proxy config
// Copy this file to `config/env.local.js` and customize as needed.

module.exports = {
  // API proxy target for '/api' during local development
  // e.g. 'http://localhost:7001' or 'http://dev.example.com:7001'
  DEV_API_TARGET: 'http://your-api-host:7001'
}

